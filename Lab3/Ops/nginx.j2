daemon            off;
worker_processes  1;
user              root;

events {
    use           epoll;
    worker_connections  1024;
}

error_log          logs/error.log;

http {
    upstream backend {
        {% for host in groups['prod'] %}
        server {{host}};
        {% endfor %}
    }
}